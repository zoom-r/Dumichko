function checkLetters(row){
    let wordOfTheDay;
    let wordOfTheDayLetters = wordOfTheDay.split();
    let enteredWord;
    let enteredWordLetters = enteredWord.split();
    for(let i = 0; i < 6; i++){
        if(enteredWordLetters[i] == wordOfTheDayLetters[i]){
            
        }
    }
}

module.exports = checkLetters;